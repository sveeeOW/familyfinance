{
  "name": "familyfinance",
  "version": "0.1.0",
  "private": true,
  "description": "Family Finance — многопользовательский учёт семейных финансов (backend + mobile + Telegram-бот)",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "backend": "npm run start:dev --workspace apps/backend",
    "mobile": "npm run start --workspace apps/mobile",
    "build:backend": "npm run build --workspace apps/backend"
  },
  "dependencies": {
    "resolve-from": "^5.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
